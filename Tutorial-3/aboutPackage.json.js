/* Package.json() file stores all the information about project
     like: 1.project Name
           2.project version
           3.git repository
           4.packages
           5.dependencies and there are many more.

   # you can create package.json file with ( `npm init` ) command.  

   # install packages with the help of (`npm install package_name`) command.

   # `package.json` store all the information about project and 
     `package-lock-json`stores all the information about installed packages.

   # if the `node_Module` file is deleted and you want it back
     then run(`npm install`) command to get back. And never push your
     node_module file on github.   

*/
